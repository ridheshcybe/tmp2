/*
    Ask the page for a scene graph report once the build has settled.

    Spliced into a copy of index.html by _probe.py, behind core.js, so the
    A/B runs it measures print what they actually built.
*/

HARNESS.diagAfter(1500);
